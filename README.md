All three projects use python3.13

Each project has its own README as needed

To see the filtered logs for just one project add the directory name as the last argument to `git log`

```
git log leetcode
```